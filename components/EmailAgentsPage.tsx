import React from 'react';

const EmailAgentsPage: React.FC = () => {
  // Per user request, the Email Agents page UI has been removed.
  // This component is no longer rendered in the application.
  return null;
};

export default EmailAgentsPage;
